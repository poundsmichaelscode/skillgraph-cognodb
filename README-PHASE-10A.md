# Phase 10A — Deterministic API Tests

Adds HTTP integration tests for health, validation, search, graph traversal, career analysis, missing entities and sanitized errors. Repository and health dependencies are mocked, so the suite does not require CognoDB credentials.

The package also aligns the frontend learning-resource graph type with the backend `resource` contract.
