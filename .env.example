# Application
NODE_ENV=development
API_PORT=4000
WEB_ORIGIN=http://localhost:3000

# CognoDB — backend only
COGNODB_URI=
COGNODB_USERNAME=cognodb
COGNODB_PASSWORD=

# Frontend — safe to expose in the browser
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
